---
- name: Install packages for app server
  hosts: dbs
  become: true

  tasks:
    - name: Install packages
      apt:
        name: ["mongodb"]

    - name: Start MongoDB
      systemd:
        name: mongodb
        enabled: true
        state: "started"
